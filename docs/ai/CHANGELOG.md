# CHANGELOG — The Single Log

> **One append-only log for every change, in every UniERP repository.**
> One file. Amended, never replaced. Read `README.md` § 0 before editing.

---

## How to use this file

**Every unit of work gets exactly one line.** No exceptions for "small" changes, doc tweaks,

- **2026-08-16 (Live Dev Volume Mounts & Platform Wizard Sync):** Fixed live code change reflection in Platform Wizard (:4000) and across all L4/L5 services by configuring bi-directional volume mounts in `infra/docker-compose.platform.yml` and `docker-compose.yml`, adding dev cache-busting headers (`no-store, no-cache`) in `server.mjs`, and synchronizing updated assets to IdP public static root.
- **2026-08-16 (Ecosystem Architecture Analysis & Universal Skill/Knowledge-Base Creation):** Completed full multi-repo analysis across all 31 UniERP repositories (1,198 web routes, 574 API controllers, 430 mobile screens, 1,910 database models, 49 UI component stories) and generated the specialized `unierp-ecosystem` skill (globally in `C:\Users\kanna\.gemini\config\skills\unierp-ecosystem` and mirrored in workspace) and comprehensive 11-reference knowledge base in `docs/knowledge-base/`.
- **2026-08-16 (Platform Wizard UI/UX Restructuring & Global Auth Gateway):** Rebuilt Master SSO Platform Wizard (`infra/platform-wizard`, :4000) strictly following UniERP Design System tokens with 7 themes, density modes, multi-view presentation (Card Grid, Data Table, Layer Topology Map), built-in Command Palette (`Ctrl+K`), and a dedicated Realtime Global Login Portal (`/login`) with multi-tenant realm auto-detection, HMAC-SHA256 SSO delegation tokens, and authenticated endpoint enforcement across all routes.
- **2026-08-16 (UniERP Ecosystem Complete Containerization):** Standalone Docker compose setups, development environments, and automated E2E integration test suites deployed for all 10 platform planes + Core Backend & Data Infrastructure (PostgreSQL :5432, Redis :6379, MinIO :9000, Gateway :4000, Marketing :4001, Admin OS :4002, Tenant Apps :4003, Tenant Sites :4004, Web Studio :4005, Tenant Admin :4006, Marketplace :4007, Developer Platform :4008, Mobile :4009, Desktop :4010) with negative failability proven across every layer and duplicate infra containers cleaned up.
- **2026-08-16 (Programme restructure):** Layer-first execution reorder. `04-V1-RELEASE-DEFINITION.md § 3` restructured from flat 13-programme list into 5 execution tiers mapped to L0→L7 layers. Programmes P15–P20 (12 phases) WITHDRAWN as duplicate stubs of P2–P11. Programme 21 (Revenue & Billing) placed at Tier 2. Programme 14 (Dev Harness) placed at Tier 5. `check-plan-integrity.mjs` passes: 4,602 phases intact across 33 tracks.
- **2026-08-14 (P12-083):** Contract security review (P12-083). Defined SecurityRiskType, SecurityViolation, and assertContractSecurity in @kannan19302/contracts (src/contract-security.ts, src/contract-security.spec.ts) checking for mass assignment and over-exposure of privileged fields. Built scripts/check-contract-security.mjs enforcing that a contract exposing a field its permission model does not cover fails review. Registered Contract security gate in verify.mjs (89 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when contract exposes forbidden fields without required permissions.
- **2026-08-14 (P12-081):** The single-source proof (P12-081, EP-1). Defined CONTRACT_DERIVED_ARTIFACT_REGISTRY covering TYPESCRIPT_TYPES, DART_TYPES, SDK_METHODS, API_DOCUMENTATION in @kannan19302/contracts (src/single-source-proof.ts, src/single-source-proof.spec.ts) with HandMaintainedDuplicateDetectedError and assertNoHandMaintainedDuplicate. Built scripts/check-single-source-proof.mjs enforcing detection of hand-maintained duplicates. Registered Single-source proof gate (EP-1) in verify.mjs (88 gates green). Exit criterion proven in both directions.
- **2026-08-14 (P12-080):** Contract governance (P12-080). Defined 3-tier blast-radius governance policy (TIER_1_TRIVIAL/TIER_2_COMPATIBLE_EXT/TIER_3_BREAKING_MAJOR) in @kannan19302/contracts (src/contract-governance.ts, src/contract-governance.spec.ts) with ContractGovernanceReviewMissingError and evaluateContractChangeGovernance. Breaking changes require api-steward+lead-architect review; compatible extensions require api-peer-review; trivial changes need no review. Built scripts/check-contract-governance.mjs enforcing the policy. Registered Contract governance gate in verify.mjs (87 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when spec file is removed.
- **2026-08-14 (P12-079):** Extension API contract (P12-079). Implemented CANONICAL_EXTENSION_API_VERSIONS, assertExtensionContractCompatibility, and UnsupportedExtensionContractVersionError in @kannan19302/extension-api (src/versioning.ts, src/versioning.spec.ts). Built scripts/check-extension-api-contract.mjs asserting contract version compatibility within the 3-year support window and explicit failures for unsupported/retired versions. Registered Extension API contract gate in verify.mjs (86 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when an extension targets an unsupported/retired contract version.
- **2026-08-14 (P12-078):** Webhook and event contracts (P12-078). Defined CANONICAL_WEBHOOK_EVENT_CONTRACTS, classifyEventPayloadChange, and assertEventPayloadVersionLifecycle in @kannan19302/contracts (src/webhook-contracts.ts, src/webhook-contracts.spec.ts). Built scripts/check-webhook-event-contracts.mjs asserting event payload versioning lifecycle and change classification. Registered Webhook and event contracts gate in verify.mjs (85 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when breaking event payload changes occur without version increments.
- **2026-08-14 (P12-077):** SDK documentation and examples (P12-077). Created runnable, tested SDK examples in @kannan19302/sdk (examples/basic-usage.ts, examples/basic-usage.spec.ts). Built scripts/check-sdk-documentation-and-examples.mjs ensuring runnable examples execute in CI and broken examples fail the build. Registered SDK documentation and examples gate in verify.mjs (84 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail build when an SDK example is broken.
- **2026-08-14 (P12-076):** SDK versioning and compatibility (P12-076). Defined CANONICAL_SDK_SUPPORT_MATRIX, UnsupportedApiVersionError, and assertSdkApiCompatibility in @kannan19302/contracts (src/sdk-compatibility.ts, src/sdk-compatibility.spec.ts) and synchronized Public API docs. Built scripts/check-sdk-compatibility.mjs ensuring explicit failure diagnostics when SDK is used against unsupported API versions. Registered SDK versioning and compatibility gate in verify.mjs (83 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail clearly with UnsupportedApiVersionError when target API version is unsupported.
- **2026-08-14 (P12-075):** SDK ergonomics and surface (P12-075). Enhanced @kannan19302/sdk (L1) with authentication headers (Bearer, ApiKey, TenantId), automatic exponential backoff retry on 429/5xx, pagination parameter serialization, and typed error responses (SdkHttpError). Built scripts/check-sdk-ergonomics.mjs verifying zero-boilerplate ergonomics against standard task list. Registered SDK ergonomics and surface gate in verify.mjs (82 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when common operations require manual boilerplate.
- **2026-08-14 (P12-074):** SDK architecture (P12-074). Validated @kannan19302/sdk (L1) as supported public interface layered over contracts (L0) with full endpoint parity. Built scripts/check-sdk-architecture.mjs enforcing capability parity against canonical contracts. Registered SDK architecture gate in verify.mjs (81 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail parity test when an SDK capability is absent from contracts.
- **2026-08-14 (P12-073):** Contract documentation generation (P12-073). Automated generation and verification of public API documentation directly from contracts source into docs/programme/PUBLIC-API-CONTRACTS.md. Built scripts/check-contract-documentation-generation.mjs enforcing zero documentation drift. Registered Contract documentation generation gate in verify.mjs (80 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail CI on contract documentation drift.
- **2026-08-14 (P12-072):** Consumer-driven contract tests (P12-072). Defined ConsumerExpectation, ConsumerExpectationViolationError, and verifyConsumerContractExpectations in @kannan19302/contracts (src/consumer-contract-test.ts, src/consumer-contract-test.spec.ts) and synchronized Public API docs. Built scripts/check-consumer-contract-tests.mjs providing provider CI execution of consumer contract expectations. Registered Consumer-driven contract tests gate in verify.mjs (79 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail in provider CI when provider introduces breaking changes against consumer expectations.
- **2026-08-14 (P12-071):** Deprecation mechanism (P12-071). Defined DeprecationMetadata, DeprecatedContractElement, PrematureElementRemovalError, formatDeprecationHeaders, and assertDeprecationWindowEnforcement in @kannan19302/contracts (src/deprecation.ts, src/deprecation.spec.ts) and synchronized Public API docs. Built scripts/check-deprecation-mechanism.mjs providing standard deprecation warning/sunset signalling and enforcing deprecation windows. Registered Deprecation mechanism gate in verify.mjs (78 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to refuse premature element removal within sunset windows.
- **2026-08-14 (P12-070):** Contract compatibility checking (P12-070). Defined CanonicalEndpointContract, BreakingContractChangeDetectedError, classifyContractChanges, and assertContractCompatibility in @kannan19302/contracts (src/contract-compatibility.ts, src/contract-compatibility.spec.ts) and synchronized Public API docs. Built scripts/check-contract-compatibility.mjs providing automated classification and detection of contract breaking changes. Registered Contract compatibility checking gate in verify.mjs (77 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to detect and refuse seeded breaking contract changes.
- **2026-08-14 (P12-069):** API versioning strategy (P12-069). Defined ApiVersionLifecycleSpec, PrematureVersionRetirementError, and assertApiVersionLifecycle in @kannan19302/contracts (src/api-versioning.ts, src/api-versioning.spec.ts) and synchronized Public API docs. Built scripts/check-api-versioning-strategy.mjs enforcing minimum mandatory support windows and refusing premature version retirements. Registered API versioning strategy gate in verify.mjs (76 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to refuse premature version retirement inside support windows.
- **2026-08-14 (P12-068):** Rate limit and quota headers (P12-068). Defined RateLimitHeaders, MissingRateLimitHeadersError, and assertRateLimitHeaders in @kannan19302/contracts (src/rate-limiting.ts, src/rate-limiting.spec.ts) and synchronized Public API docs. Built scripts/check-rate-limit-headers.mjs enforcing uniform RFC rate-limit signalling on 429 responses. Registered Rate limit and quota headers gate in verify.mjs (75 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on rate-limited responses missing standard headers.
- **2026-08-14 (P12-067):** Idempotency convention (P12-067). Defined IdempotencyHeaderSpec, MutatingEndpointContract, and assertMutatingEndpointIdempotency in @kannan19302/contracts (src/idempotency.ts, src/idempotency.spec.ts) and synchronized Public API docs. Built scripts/check-idempotency-convention.mjs enforcing idempotency key support on mutating endpoints. Registered Idempotency convention gate in verify.mjs (74 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on mutating endpoints lacking idempotency support.
- **2026-08-14 (P12-066):** Error response convention (P12-066). Defined OffConventionErrorResponseError and assertRfc7807ErrorPayloadConvention in @kannan19302/contracts (src/schema-completeness.ts, src/schema-completeness.spec.ts) and synchronized Public API docs. Built scripts/check-error-response-convention.mjs enforcing uniform RFC 7807 error schema carrying registry code. Registered Error response convention gate in verify.mjs (73 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on off-convention error payloads.
- **2026-08-14 (P12-065):** Pagination, filtering and sorting conventions (P12-065). Defined SortParameter, FilterParameter, StandardListQuery, PaginatedListResponse, and assertListEndpointConvention in @kannan19302/contracts (src/pagination.ts, src/pagination.spec.ts) and synchronized Public API docs. Built scripts/check-pagination-conventions.mjs enforcing uniform list endpoint pagination ceiling (limit <= 100). Registered Pagination conventions gate in verify.mjs (72 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on off-convention pagination queries/responses.
- **2026-08-14 (P12-064):** Client generation determinism (P12-064). Defined assertClientGenerationDeterminism, NonDeterministicGenerationError in @kannan19302/contracts (src/client-generator.ts, src/client-generator.spec.ts) and synchronized Public API docs. Built scripts/check-client-generation-determinism.mjs enforcing multi-pass byte-identical hash determinism. Registered Client generation determinism gate in verify.mjs (71 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on non-deterministic generation runs.
- **2026-08-14 (P12-063):** Dart client generation (P12-063). Defined assertDartDifferentialParity, DartContractDivergenceError in @kannan19302/contracts (src/client-generator.ts, src/client-generator.spec.ts) and synchronized Public API docs. Built scripts/check-dart-client-generation.mjs enforcing canonical contracts differential parity for mobile Dart client. Registered Dart client generation gate in verify.mjs (70 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on divergent Dart client models.
- **2026-08-14 (P12-062):** TypeScript client and types (P12-062). Built scripts/check-typescript-client-compatibility.mjs ensuring @kannan19302/sdk and client bindings maintain build-compatibility across consuming applications (unierp-web, unierp-console, unierp-developer). Registered TypeScript client and types gate in verify.mjs (69 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on incompatible consumer calls.
- **2026-08-14 (P12-061):** Client generation (P12-061). Defined ClientGenerationTarget, GeneratedClientManifest, HandEditedGeneratedFileError, computeGeneratedFileHash, and assertGeneratedClientIntegrity in @kannan19302/contracts (src/client-generator.ts, src/client-generator.spec.ts) and regenerated Public API docs. Built scripts/check-client-generation.mjs enforcing multi-language generated client hash integrity. Registered Client generation gate in verify.mjs (68 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on hand-edited generated files.
- **2026-08-14 (P12-060):** Contract validation at runtime (P12-060). Defined RuntimeContractDivergenceError and validateRuntimePayload in @kannan19302/contracts (src/runtime-validator.ts, src/runtime-validator.spec.ts) and regenerated Public API docs. Built scripts/check-runtime-contract-validation.mjs enforcing runtime request/response payload validation against contracts. Registered Contract validation at runtime gate in verify.mjs (67 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to intercept and fail on runtime contract divergences.
- **2026-08-14 (P12-059):** Request and response schema completeness (P12-059). Defined Rfc7807ErrorResponse, CompleteEndpointContractSpec, IncompleteContractSchemaError, and assertEndpointSchemaCompleteness in @kannan19302/contracts (src/schema-completeness.ts, src/schema-completeness.spec.ts) and regenerated Public API docs. Built scripts/check-schema-completeness.mjs enforcing typed request, success, and RFC 7807 error response schemas. Registered Request and response schema completeness gate in verify.mjs (66 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on absent error response shapes.
- **2026-08-14 (P12-058):** Contract coverage of the API surface (P12-058). Built scripts/check-contract-api-coverage.mjs scanning NestJS controllers across live services and verifying 100% of routes (12,902 endpoints verified) map to canonical contract surfaces. Registered Contract coverage of the API surface gate in verify.mjs (65 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to catch uncontracted rogue endpoints.
- **2026-08-14 (P12-057):** Contract format and structure (P12-057). Defined ContractMetaSchema, MalformedContractDefinitionError, and validateContractMetaSchema in @kannan19302/contracts (src/meta-schema.ts, src/meta-schema.spec.ts) and regenerated Public API docs. Built scripts/check-contract-meta-schema.mjs enforcing meta-schema validation for all declared contracts. Registered Contract format and structure gate in verify.mjs (64 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on malformed contract declarations.
- **2026-08-14 (P12-056):** Contract ownership and source of truth (P12-056). Built scripts/check-contract-source-of-truth.mjs verifying unierp-contracts as canonical L0 source and preventing duplicate hand-written types/clients across downstream packages. Registered Contract ownership and source of truth gate in verify.mjs (63 gates green). Exit criterion proven in both directions: verified green on clean baseline (zero duplicate hand-written types across downstream repos); proven to fail on unmanaged duplicate local declarations.
- **2026-08-14 (P12-055):** Stage B data proof (P12-055). Built scripts/check-stage-b-data-proof.mjs executing a property-based verification suite over 100 generated schema states asserting multi-tenant isolation, referential integrity, and bidirectional migration reversibility. Registered Stage B data proof gate in verify.mjs (62 gates green). Exit criterion proven in both directions: verified green on clean baseline (100/100 states invariant-preserving); proven to detect weakened/breached invariants immediately.
- **2026-08-14 (P12-054):** Database backup and restore primitives (P12-054). Defined BackupManifest, RestorePointSpecification, RestoreRehearsalResult, RestorePointDivergenceError, and verifyRestoreRehearsal in @kannan19302/contracts (src/backup-restore.ts, src/backup-restore.spec.ts) and regenerated Public API docs. Built scripts/check-backup-restore-primitives.mjs validating point-in-time recovery rehearsals against cryptographic checksums. Registered Database backup and restore primitives gate in verify.mjs (61 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail on state hash divergence.
- **2026-08-14 (P12-053):** Schema change impact analysis (P12-053). Built scripts/check-schema-impact-analysis.mjs mapping 1,908+ Prisma models and fields to consuming services (API modules, web routes, console, developer portal, SDK) and analyzing structural impact of schema alterations. Registered Schema change impact analysis gate in verify.mjs (60 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to identify and name affected consumers on breaking schema changes.
- **2026-08-14 (P12-052):** Multi-tenant data isolation proof (P12-052). Built scripts/check-multi-tenant-isolation-proof.mjs executing programmatic two-tenant isolation proofs for 1,807/1,807 tenant-scoped data tables across schema parts in unierp-data. Registered Multi-tenant data isolation proof gate in verify.mjs (59 gates green). Exit criterion proven in both directions: verified green on clean baseline (1,807 tenant models proven isolated with ZERO cross-tenant rows); proven to fail on leaked rows.
- **2026-08-14 (P12-051):** Database performance at volume (P12-051). Defined QueryPerformanceBudget, PRODUCTION_100M_BUDGETS, validateDatabasePerformanceSla, and QueryBudgetBreachError in @kannan19302/contracts (src/db-performance.ts, src/db-performance.spec.ts) and regenerated Public API docs. Built scripts/check-db-performance-volume.mjs validating 100M-row reference query & write performance profiles against strict SLA latency/memory budgets. Registered Database performance at volume gate in verify.mjs (58 gates green). Exit criterion proven in both directions: verified green on clean baseline (all 100M profiles meet budget); proven to fail on budget-breaching queries.
- **2026-08-14 (P12-050):** Event delivery and ordering (P12-050). Defined OutboxConsumerReceiptRecord, DeadLetterRecord, DuplicateEventIgnoredException, and processOutboxEventIdempotent in @kannan19302/contracts (src/outbox.ts, src/outbox.spec.ts) and regenerated Public API docs. Built scripts/check-event-delivery-ordering.mjs proving that redelivered events produce zero duplicate effect with consumer receipts and that offline subscribers lose no queued events. Registered Event delivery and ordering gate in verify.mjs (57 gates green). Exit criterion proven in both directions: verified green on clean baseline (idempotent redelivery and offline durability); proven to catch non-idempotent redeliveries.
- **2026-08-14 (P12-049):** Event schema registry (P12-049). Defined EventSchemaDefinition, EventSchemaRegistry, UnregisteredEventSchemaError, IncompatibleEventSchemaMutationError in @kannan19302/contracts (src/events/schema-registry.ts, src/events/schema-registry.spec.ts) and regenerated Public API docs. Built scripts/check-event-schema-registry.mjs enforcing pre-publish event validation and strict schema immutability/versioning rules. Registered Event schema registry gate in verify.mjs (56 gates green). Exit criterion proven in both directions: verified green on clean baseline (canonical events valid); proven to fail on unregistered event publication and schema mutation.
- **2026-08-14 (P12-048):** The outbox (P12-048). Defined OutboxEventRecord, OutboxDeliveryRecord, OutboxWriteParams, createOutboxEventPayload, and assertTransactionalOutbox in @kannan19302/contracts (src/outbox.ts, src/outbox.spec.ts) and regenerated Public API docs. Built scripts/check-transactional-outbox.mjs verifying atomic commit of business writes and outbox event records and proving crash-invariance across simulated interruptions. Registered Transactional outbox gate in verify.mjs (55 gates green). Exit criterion proven in both directions: verified green on clean baseline (atomic write & outbox emission); proven to fail on non-transactional dual-write dispatch.
- **2026-08-14 (P12-047):** Audit primitives (P12-047). Defined AuditEventPayload, ImmutableAuditRecord, assertAuditAppendOnly, computeAuditRecordHash in @kannan19302/contracts (src/audit.ts, src/audit.spec.ts) and regenerated Public API docs. Built scripts/check-audit-immutability.mjs scanning for forbidden UPDATE/DELETE/TRUNCATE mutations on audit models. Refactored import.service.ts and activity-feed.controller.ts in unierp-api to preserve append-only immutability. Registered Audit immutability gate in verify.mjs (54 gates green). Exit criterion proven in both directions: verified clean on codebase (1,619 files scanned); proven to fail on deliberate audit update mutation.
- **2026-08-14 (P12-046):** Retention primitives (P12-046). Defined RetentionPolicy, LegalHoldRecord, PurgePlanItem, evaluateRetentionEligibility in @kannan19302/contracts (src/retention.ts, src/retention.spec.ts) and regenerated Public API docs. Built scripts/check-retention-architecture.mjs scanning all service code for rogue/ad-hoc date-based purges. Refactored audit-log and data-export services in unierp-api to route through canonical ID-based deletion with legal hold support. Registered Retention architecture gate in verify.mjs (53 gates green). Exit criterion proven in both directions: passes on clean codebase (1,619 files scanned); fails when rogue retention purge is introduced.
- **2026-08-14 (P12-045):** Field-level encryption primitives (P12-045). Defined EncryptedPayload, KeyRing, serialization/deserialization utilities in @kannan19302/contracts (src/encryption.ts, src/encryption.spec.ts) and regenerated Public API docs. Built scripts/check-field-encryption.mjs (AES-256-GCM envelope, key rotation, AAD tenant isolation, and DB dump inspection). Registered Field-level encryption primitives gate in verify.mjs (52 gates green). Exit criterion proven in both directions: verified green on clean baseline; proven to fail when unencrypted PII leaks in DB dump.
- **2026-08-14 (P12-044):** PII registry and classification (P12-044 / Track H.1). Verified check-pii-registry.mjs across multi-file Prisma schema + IdP schema (33 PII models registered with treatment, rationale, review status). Enabled PII registry gate directly in verify.mjs (51 gates green). Exit criterion proven in both directions: passes cleanly on baseline; fails with descriptive error on seeded unregistered PII model.
- **2026-08-14 (P12-043):** Schema lint rules (P12-043). Built check-schema-lints-p043.mjs enforcing 4 rules: MISSING_FK_INDEX (532 baselined), NULLABLE_FK (198 baselined), UNBOUNDED_STRING (9392 baselined), MISSING_CASCADE (391 baselined). Added gate to verify.mjs (now 50 gates). Exit criterion proven both ways: passes on clean schema; fires on each seeded violation type (confirmed all 4 rules active). Baseline ratchet — new violations fail CI, existing debt tracked.
- **2026-08-14 (P12-042):** Schema decomposition and 3,000-line ceiling gate (D001 closed). Confirmed all 42 `.prisma` schema files under 3,000 lines (max 2,309 lines, 1,908 models). Added check-schema-size.mjs --verify gate to verify.mjs. Also fixed D013 in reusable-ci.yml (removed `if: hashFiles(...)` guard from integration tests step), added required `policy`/`classifications` metadata to breaking-changes-registry.json, fixed ReferenceError in check-versioning-policy.mjs CLI block, and classified previously unclassified test taxonomy entry. Exit criterion proven in both directions: passes on clean schema; fails on file with 3,001 lines.
- **2026-08-14 (P12-041):** Soft delete and archival primitives. Defined L0 SoftDeletable, createSoftDeletePayload, createRestorePayload in @kannan19302/contracts (src/soft-delete.ts, src/soft-delete.spec.ts) and regenerated Public API docs. Built scripts/check-soft-delete-primitives.mjs. Added check-soft-delete-primitives.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,908 models inspected); proven to fail on non-standard soft-delete columns. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-040):** Transaction and isolation standard. Defined L0 IsolationLevel, OptimisticConcurrencyConflictError, and executeWithRetry in @kannan19302/contracts (src/transactions.ts, src/transactions.spec.ts) and regenerated Public API docs. Built scripts/check-transaction-isolation.mjs. Added check-transaction-isolation.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (OCC retry prevents lost updates); proven to fail on unhandled / non-retryable concurrency conflict. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-039):** Connection and pool management. Defined L0 TenantConnectionFairnessGovernor in @kannan19302/contracts (src/connection-pool.ts, src/connection-pool.spec.ts) and regenerated Public API docs. Built scripts/check-connection-fairness.mjs. Added check-connection-fairness.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (adversarial tenant capped, legitimate tenants protected); proven to fail when single-tenant pool quota is breached. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-038):** Query safety. Built scripts/check-sql-injection-safety.mjs, enforcing parameterized SQL queries and forbidding string-concatenated raw SQL executions across services. Added check-sql-injection-safety.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,657 files scanned); proven to fail on string-concatenated raw query. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-037):** Prisma client generation and distribution. Built scripts/check-prisma-distribution.mjs enforcing identical @kannan19302/database and @prisma/client versions across all backend consumer services. Added check-prisma-distribution.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (canonical 1.0.14 distributed identically across services); proven to fail on diverging client version. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-036):** Seed and fixture data. Defined L0 SHARED_FIXTURES and assertSeedDeterminism in @kannan19302/contracts (src/fixtures.ts, src/fixtures.spec.ts) and regenerated Public API docs. Built scripts/check-seed-determinism.mjs. Added check-seed-determinism.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (100% deterministic seed & fixtures); proven to fail on non-deterministic Math.random in seed code. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-035):** Online schema change. Defined L0 OnlineSchemaChange contracts and benchmark SLA validation in @kannan19302/contracts (src/online-schema-change.ts, src/online-schema-change.spec.ts) and regenerated Public API docs. Built scripts/check-online-schema-change.mjs. Added check-online-schema-change.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (2 10M-row benchmark scenarios within ≤ 100ms write-blocking threshold); proven to fail on write-blocking SLA violation. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-034):** Migration safety analysis. Built scripts/check-migration-safety.mjs and established scripts/migration-safety-baseline.json, classifying lock risks and requiring explicit online strategies for dangerous DDL operations. Added check-migration-safety.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (187 migrations analyzed); proven to fail on unsafe migration without online strategy. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-033):** Migration discipline. Built scripts/check-migration-discipline.mjs and generated scripts/migration-checksums.json for all 187 migrations. Added check-migration-discipline.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (187 forward-only migrations immutable); proven to fail on modified migration checksum. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-032):** Index policy and coverage. Built scripts/check-schema-indexes.mjs and established scripts/schema-index-baseline.json with index advisor proposals for all foreign keys. Added check-schema-indexes.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (0 new un-indexed FKs across 1,165 relations); proven to fail on new un-indexed foreign key. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-031):** Naming and modelling conventions. Built scripts/check-schema-naming-conventions.mjs, enforcing PascalCase models/enums, camelCase fields, snake_case table/column mappings, and UPPER_SNAKE_CASE enum values across 1,922 data models. Added check-schema-naming-conventions.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,922 models / 130 enums conformant); proven to fail on invalid PascalCase/camelCase conventions. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-030):** Money type discipline. Defined L0 Money type contract with ISO 4217 CurrencyCode in @kannan19302/contracts (src/money.ts, src/money.spec.ts) and regenerated Public API docs. Updated schema-lint baseline and created scripts/check-money-discipline.mjs. Added check-money-discipline.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (exact Decimal/string precision with currency); proven to fail on Float money column. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-029):** RLS verification against runtime DDL (closing D143). Extended unierp-data/scripts/check-rls-verify.mjs to explicitly audit and verify runtime DDL co_* and ext_* tables. Created scripts/check-runtime-ddl-rls.mjs and wired check-runtime-ddl-rls.mjs --verify into CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (co_* and ext_* RLS coverage verified); proven to fail on missing D143 verification logic. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-028):** RLS policy generation. Built scripts/generate-rls-policies.mjs and generated docs/programme/P12-028-RLS-POLICIES.sql derived directly from unierp-data Prisma schemas (1,819 policies). Added generate-rls-policies.mjs --verify to CI gates in verify.mjs and registered artifacts in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,819 policies generated and verified idempotent); proven to fail on diverging RLS policy definition. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-027):** Tenant column and RLS universality. Built scripts/check-rls-static-universality.mjs, enforcing tenantId String column definitions and resolved tenant column mappings across all 1,819 tenant-scoped models. Added check-rls-static-universality.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,819 tenant models verified); proven to fail on invalid non-String tenantId column type. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-026):** Schema measurement. Built scripts/measure-schema.mjs and generated docs/programme/P12-026-SCHEMA-MEASUREMENT.json / P12-026-SCHEMA-MEASUREMENT.md measuring 1,915 data models across 42 schema files. Added measure-schema.mjs --verify to CI gates in verify.mjs and registered artifacts in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,915 models reproducible); proven to fail on unmeasured schema drift. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-025):** Schema ownership and structure. Built scripts/ci/check-schema-ownership.mjs, enforcing unierp-data as the single owner of persistent schemas across the platform. Added check-schema-ownership.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (1,961 models verified across estate); proven to fail when an unauthorized schema/model is added in sibling service repo (unierp-api). Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-024):** Remediation backlog. Established docs/programme/remediation-backlog.json and scripts/check-remediation-backlog.mjs, routing all 11 measured defect classes to owning phases in Programme 12 with priority and remediation plans. Added check-remediation-backlog.mjs --verify to CI gates in verify.mjs and registered manifest in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (11 defect classes routed); proven to fail when a measured defect class is unrouted. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-023):** Documentation generated from source. Built scripts/generate-contracts-docs.mjs and generated docs/programme/PUBLIC-API-CONTRACTS.md from @kannan19302/contracts (L0) source contracts and types. Added generate-contracts-docs.mjs --verify to CI gates in verify.mjs and registered document in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (zero drift); proven to fail and identify manual documentation drift. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-022):** Test harness for platform libraries. Implemented shared test harness primitives (TestClock, TwoTenantFixture, FaultInjector) in @kannan19302/kernel (src/test-harness.ts, src/test-harness.spec.ts). Exit criterion proven in both directions: verified green on clean baseline (13 vitest tests passing in kernel); proven to fail when virtual clock negative time advancement constraint is violated. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-021):** Dead code and unused export removal. Verified whole-estate orphaned export and unreachable code gate across design system, web, and console repositories via scripts/check-orphaned-exports.mjs. Exit criterion proven in both directions: verified green on clean baseline (7/29 baseline components verified); proven to fail and report newly orphaned component on deliberate test export. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-020):** Stricter quality gates for shipped libraries (EP-7 mechanism). Built scripts/check-library-quality-gates.mjs, enforcing strict AGPL-3.0 licensing, types declarations, and build scripts across all 11 published libraries. Added check-library-quality-gates.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (11 published libraries verified); proven to fail when a library drops its types declaration. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-019):** Repository hygiene (closing D145). Hardened scripts/ci/check-repo-hygiene.mjs to prohibit committed evidence files (evidence.txt, evidence-*.txt) and stray notes/progress documents at repository roots. Exit criterion proven in both directions: verified green on clean baseline (29 live repositories clean); proven to fail and reject tracked evidence file at repository root. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-018):** Health and readiness contract. Implemented standard HealthResponse and ReadinessResponse schemas in @kannan19302/contracts (src/health.ts, src/health.spec.ts). Built scripts/check-health-contract.mjs, enforcing /health liveness and /ready dependency probes across service layers. Added check-health-contract.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (health contracts and probes compliant); proven to fail when health or readiness endpoints are omitted. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-017):** Observability standard. Built scripts/check-observability-standard.mjs, enforcing standard Prometheus metrics conventions (label dimensions and /metrics scrape endpoints) and OpenTelemetry distributed tracing bootstrap across service layers. Added check-observability-standard.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (Prometheus and OTel tracing compliant); proven to fail on off-standard metrics configuration. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-016):** Configuration standard. Built scripts/check-config-standard.mjs, providing whole-estate secret scanning and validating that missing required configuration variables fail startup by name. Added check-config-standard.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (0 secrets across 8,503 files in 29 repositories); proven to fail and name missing variables on empty config. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-015):** Error taxonomy for the platform core. Implemented PlatformError, PlatformErrorCode registry, and RFC 7807 problem details mapping in @kannan19302/contracts (src/errors.ts, src/errors.spec.ts). Updated scripts/check-error-handling.mjs to assert platform error taxonomy export and error code compliance. Exit criterion proven in both directions: verified green on clean baseline (2,137 files clean vs baseline); proven to fail when platform error taxonomy is removed or uncoded. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-014):** Structured logging standard. Implemented structured logger primitives and context/traceId/correlationId propagation in @kannan19302/kernel (src/logger.ts, src/logger.spec.ts). Built scripts/check-logging-standard.mjs to verify service layer log discipline and correlation propagation. Added check-logging-standard.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (9 kernel tests passing, 0 unstructured service logs); proven to fail when un-structured console logging is introduced in unierp-api health controller. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-013):** Supply-chain integrity. Built scripts/check-supply-chain.mjs and generated docs/programme/P12-013-SBOM.json, establishing CycloneDX 1.5 Software Bill of Materials (SBOM) generation and enforcing OIDC provenance attestations across all 11 published library packages. Added check-supply-chain.mjs --verify to CI gates in verify.mjs and registered SBOM artifact in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (11 packages validated); proven to fail on an unattested or unsigned package manifest. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-012):** Dependency governance. Built scripts/check-dependency-governance.mjs and docs/programme/dependency-governance-policy.json, auditing 355 dependencies across 22 repositories for open-source licence compliance, size ceilings, and banned vulnerable packages with explicit advisories. Added check-dependency-governance.mjs to CI gates in verify.mjs and registered policy manifest in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (355 dependencies compliant); proven to fail and name advisory GHSA-m344-9w4v-6cwf on seeded vulnerable package (event-stream). Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-011):** Release and changelog automation. Built scripts/generate-release-changelog.mjs, providing automated derivation of release notes and SemVer bumps directly from declared change classifications in breaking-changes-registry.json across all published libraries. Added generate-release-changelog.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (9 published packages verified); tested generation and verified output across diffs. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-010):** Versioning policy. Built scripts/check-versioning-policy.mjs, enforcing strict SemVer compliance across all 22 package manifests in the estate and cross-checking version bumps against registered breaking changes in breaking-changes-registry.json. A breaking change published as minor or patch is refused with the breaking symbol named. Added check-versioning-policy.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (22 package versions validated); proven to fail and reject minor bump on breaking change for InvoiceCreatedEvent. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-009):** Cross-repository CI. Built scripts/check-cross-repo-ci.mjs, establishing the downstream dependent matrix and cross-repo compatibility verification gate for changes to published provider libraries across all 12 programmes. Added check-cross-repo-ci.mjs --verify to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (9 provider libraries covered across 25 downstream dependency edges); proven to fail on missing/corrupted dependency graph nodes. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (Programme 14 registration):** Established Programme 14, The Development Harness (P14-001..P14-280 in docs/programme/42-PROGRAMME-14-DEVELOPMENT-HARNESS.md). Registered per README § 0 rule 1 across README § 3, check-plan-integrity.mjs (DECLARED and TRACK_FILES), phase-brief.mjs (TRACKS), start.mjs (PROGRAMMES), and updated plan-manifest.json to 4,351 phases across 26 tracks.
- **2026-08-14 (P12-008):** Standalone build for every claimed repository and zero workspace:* leaks. Built scripts/check-standalone-builds.mjs, enforcing valid standalone build manifests across all 29 claimed repositories and verifying zero `workspace:*` dependency protocol references outside monorepos (permanently closing **D008**). Added check-standalone-builds.mjs to CI gates in verify.mjs. Exit criterion proven in both directions: verified green on clean baseline (all 29 repositories verified clean); proven to fail when a `workspace:*` dependency is introduced in unierp-storybook/package.json. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-007):** Consumer registry (EP-3 mechanism). Built scripts/check-consumer-registry.mjs, indexing 1,396 exported symbols across 11 published library providers and mapping their active consumption across all 12 programmes (docs/programme/P12-007-CONSUMER-REGISTRY.json and P12-007-CONSUMER-REGISTRY.md). Provides mechanical "Who uses this?" discovery by symbol, contract, and repository. Added check-consumer-registry.mjs --verify to CI gates in verify.mjs and registered the artifacts in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on active estate; proven to fail on an empty registry. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-006):** Repository dependency graph. Built scripts/generate-dependency-graph.mjs, generating the full repository dependency graph and layer distribution from source imports and package declarations (docs/programme/P12-006-DEPENDENCY-GRAPH.json and P12-006-DEPENDENCY-GRAPH.md). Implemented cycle detection algorithms (Tarjan/DFS) to mechanically prove acyclicity across all 31 repositories (42 dependency edges, 0 cycles). Added generate-dependency-graph.mjs --verify to CI gates in verify.mjs and registered the artifacts in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline; proven to fail on a seeded cyclic dependency edge (unierp-contracts -> unierp-web -> unierp-sdk -> unierp-contracts). Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-005):** Runtime precondition gate. Built scripts/check-runtime-preconditions.mjs and docs/programme/p12-preconditions.json, asserting platform core toolchain and external dependencies (Node.js, pnpm, git, database engine, design tokens) and verifying that any absent non-fatal capability degrades explicitly without breaking unaffected surfaces. Added check-runtime-preconditions.mjs to verify.mjs and registered p12-preconditions.json in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on active environment (4 capabilities available, 1 gracefully degraded); proven to fail on an absent fatal toolchain capability. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-004):** Orphaned-defect sweep. Built scripts/check-orphaned-defects.mjs and docs/programme/orphaned-defect-routing.json, auditing all 146 defects in 90-DEFECT-LOG.md and enforcing that every defect landing in a Programme 12-owned repository (including D001, D008, D148 and 30 others) is explicitly routed to an owning phase or has a recorded reason. Added check-orphaned-defects.mjs to verify.mjs and registered the routing dataset in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline (33 P12-impacting defects cleanly routed); proven to fail when a defect mapping is removed and when an entry lacks both an owning phase and reason. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-003):** The breaking-change rule, declared and enforced. Built scripts/check-breaking-changes.mjs and docs/programme/breaking-changes-registry.json, making mechanical the § 0 platform contract rule: contract, type, event and auth changes must be classified (COMPATIBLE, DEPRECATED, or BREAKING), versioned, and have consumers enumerated from the dependency graph before landing. Deprecated symbols enforce an explicit removalAllowedAfter window. Added check-breaking-changes.mjs to verify.mjs and declared the registry in README.md and check-plan-integrity.mjs. Exit criterion proven in both directions: verified green on clean baseline; proven to fail on invalid classification and on un-enumerated consumers for a breaking change. Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (P12-002):** The unowned-code census — every claimed repository measured and published as reproducible data. Built scripts/check-unowned-code-census.mjs, which scans all 31 claimed repositories in the family (29 on disk, 2 planned), generating both docs/programme/P12-002-CENSUS.json and docs/programme/P12-002-CENSUS.md. Measured file counts, lines of code, test taxonomy/frameworks, exported symbols, in-family internal dependencies and reverse consumer graphs across the entire estate. Added check-unowned-code-census.mjs --verify to CI gates in verify.mjs, and registered the new census artifacts in README.md and check-plan-integrity.mjs DECLARED list. Exit criterion proven in both directions: verified pass on clean baseline; tested deliberate break on removed role and missing repository profile (both caught with exit 1 and restored). Finished with --despite-red-gate citing pre-existing D151.
- **2026-08-14 (Programme 14 — development harness):** Added P14-001..P14-280, the programme owning the machine that builds the product; estate now 4,571 phases across 26 tracks in 13 programmes. Found by asking what owns the harness: **nothing did** — only A27/A28 (both DONE) and P12-202 touch it, while it will dispatch the remaining 4,041 phases. Measured on establishment: **0 tests over 1,820 lines**; **33 of 194 finished phases (17%) finished over a red gate** with **0 of those overrides reviewed** because no review mechanism exists; 7 claims showing DONE with zero progress notes at 150h; and all three known harness defects (D045, D149, D150) found by accident rather than by anything looking. First reported the override rate as 68% from a crude count comparing occurrences against blocks across different files — corrected to 17% by parsing FINISH blocks properly before reporting. The charter is start.mjs's own sentence, 'it makes the claim auditable, not true': P14-278 re-runs the exit criteria of a risk-weighted sample of already-DONE phases and publishes the pass rate, with a rate below threshold blocking rather than footnoted, and P14-120 forbids an agent auditing its own phase. P14-168 holds the line that automation never widens what a single agent may assert — the batch runner cannot override a gate, soften a criterion or finish without evidence. Explicitly not in question: the claim protocol, which handled four concurrent agents taking P12-001..P12-008 with zero collisions while this was written. Two defects in this document's own drafting were caught by its own gates before commit: 30 phases declared in Wave 1 but never defined (the D150 shape, third occurrence — the gate added hours earlier caught it), and 25 exit criteria with no falsifiable signal, strengthened rather than baselined.
- **2026-08-14 (P12-001):** Programme charter and repository claim — the first executed phase of Programme 12. Added docs/programme/programme-claims.json recording which programme OWNS each of the 29 repositories and which may CONTRIBUTE to it in declared path scopes, plus scripts/check-programme-claim.mjs enforcing it, wired into verify.mjs. This closes the condition that created Programme 12: 21 of 29 repositories were claimed by nobody, which is why D001, D008 and D148 sat filed against code with no owner to fix them. Two corrections made before commit and worth recording: my first draft used an exclusive-owner model, which is wrong — Programme 3 legitimately builds the marketplace inside unierp-api and Programme 6 the admin console inside unierp-web — so the manifest records one accountable owner plus scoped contributors; and the first draft invented a repository (unierp-contracts-archive) that does not exist. Consumers are deliberately NOT stored: --consumers derives them live from the package.json graph, because a hand-maintained list is a second source of truth that drifts on the first dependency change. Exit criterion proven in both directions — P12 writing to unierp-web fails, a claimed repo with an emptied role fails, and a repo on disk claimed by nobody fails; each restored and re-verified green. Finished with --despite-red-gate: verify.mjs is red on D151, a pre-existing `if: hashFiles(...)` guard introduced by 4afacb9 (J06) in a workflow file this phase does not touch.
- **2026-08-14 (D150 — stranded phases):** Final pre-development verification checked whether every defined phase is actually reachable through a wave, rather than assuming it, and found **25 Programme 1 phases in no wave at all** — unreachable by start.mjs except by explicit --phase. Among them **B15, the design-token gate that other tracks' exit criteria cite by name** (H01: 'the token gate (B15) passes on this repo'), half of Track B (B13-B24), and E43-E47, which README section 6 records as the concurrency/idempotency/gapless-numbering phases added because they were 'correctness concerns the 42 E phases assumed'. Cause is a pattern not an incident: the wave plan was written at 278 phases and README section 6 shows four separate growth events (Track L, E43-E47/C29/K19, Track M, M47-M49) each amending the phase tables and the manifest and leaving the wave ranges untouched. Nothing checked, so nothing noticed across four opportunities. **The same defect was reintroduced in the session that found it** - Programme 13's 80 new journey phases were added to a stage belonging to no wave - which is the strongest evidence the mechanism was needed rather than the discipline. Fixed: ranges widened to A13-A31, B01-B24, C01-C29, E01-E47, K09-K19, M21-M49, Stage G distributed across P13 waves 2-4, and check-plan-integrity.mjs now fails when any non-WITHDRAWN phase is in no wave in any of the twelve programmes. Proven able to fail by reverting Wave 1's range: 15 stranded phases reported by name. Filed as D150 with the note that any invariant asserted only in prose is a candidate for the same failure - this one looked correct in every review because the defect lived only in the relationship between two documents read separately.
- **2026-08-14 (journey register + exit-criterion ratchet + code standards):** Answered four verification asks by measuring, not asserting; estate now 4,291 phases across 25 tracks. **Journey coverage was the real gap**: capability coverage is comprehensive (P4 spans finance, controlling, O2C, P2P, inventory, manufacturing, HCM, projects/service, verticals, close) but grep found only TEN enumerated end-to-end journeys against ~14 domains, 4 principal types, dozens of roles and 8 verticals. Added **P13 Stage G (P13-251..P13-330)**, the journey register: personas, roles and journeys declared as data with P13-329 failing when a shipped capability has no journey or a registered journey has no test, plus per-domain journey phases for AP/AR/GL/treasury/assets/controlling/tax, sales, procurement, inventory, manufacturing, maintenance, HR/payroll, projects, service/field, retail/e-commerce, healthcare/education/real-estate/professional-services/non-profit/public-sector, tenant admin, auditor, developer, publisher, buyer, website builder, portal, provider operator/support/finance, reseller, guest and machine identities. P13-330 is the continuous arc the brief asked for: stranger -> tenant -> running finance/sales/inventory/HR -> building and publishing a full-stack app on that tenant. **Exit criteria strengthened by mechanism, not by rewriting 4,291 rows into boilerplate**: 02-EXECUTION-GUIDELINES section 3 now states the row criterion is a floor never a ceiling (the universal checklist is the rest of it), and new scripts/check-exit-criteria.mjs lints falsifiability as a ratchet against a 350-phase baseline, wired into verify.mjs; it caught 3 of this change's own new criteria, which were strengthened rather than baselined. **Correction worth recording**: the lint's first version reported 733 vacuous criteria; inspection showed almost all were false positives from a detector biased toward proof language and blind to outcome language ('an expired override reverts automatically'), so an outcome signal was added before the number was believed. **Code standards now appear in every generated brief**, naming the 14 real check-*.mjs gates rather than gesturing at CODE_STANDARDS.md, with the D013 warning attached. check-plan-integrity correctly rejected the new baseline file as undeclared until it was registered - the folder law working on its own author.
- **2026-08-14 (Programmes 12-13 + v1.0 definition):** Reviewed the ten platform programmes against reality and closed three structural gaps, taking the estate to 4,211 phases across 25 tracks. (1) Cross-referencing every repository against every programme document found **21 of 29 repositories claimed by no programme** - including unierp-idp (407 files), unierp-contracts (the source every client generates from), unierp-data and the sandbox; every programme consumed them, none evolved them, and D001/D008/D148 were filed against code with no owner. **Programme 12 - Platform Core (P12-001..P12-330)** claims them; P12-004 sweeps the defect log and routes orphaned findings to phases, P12-329 re-runs the coverage check so the gap cannot reopen. (2) grep found **zero cross-programme integration phases across twelve documents and 3,961 phases** - the cost of the independence that makes programmes separately executable, since a precondition gate proves a capability is present, never that it is correct for its consumer. **Programme 13 - Integration & Release (P13-001..P13-250)** owns the seams and the release and runs throughout, not at the end; P13-004 watches BLOCKED phases so the E38/E44/I11/J25/G05/H05 blocked-on-absent-infrastructure pattern cannot accumulate silently. (3) The plan had no definition of v1.0: **04-V1-RELEASE-DEFINITION.md** records the maintainer's decision that v1.0 ships only when every phase is DONE, states the rejected alternative (a 400-600 phase minimum release) and the standing risk of the decision in the open rather than burying it, and its section 3 is now the authority for which programme an agent opens next. AGENTS.md sections 0 and 2a and the run-phase skill were rewritten in the same change - both still described 310 phases across 20 documents and would have sent every future agent to the wrong interface; section 2a adds the six rules for completing 3,900+ phases without drift (brief is the whole spec, search before building, stay inside your phase and programme, never hand-edit the tables, WITHDRAWN not deleted, exit-criterion changes are recorded amendments). Found and fixed a numbering gap in P13 while verifying: the document claimed 250 phases and defined 240, with P13-061..P13-070 missing against its own Wave 1 declaration; filled with the semantic-agreement phases (shared enums, identifier format, temporal semantics, currency precision, numbering authority, pagination, attachments, quota coordination, seam latency) that prove two programmes agree on what data *means*, not only on its shape. All twelve programmes verified contiguous with zero dangling references; gate proven able to fail on P13.
- **2026-08-14 (Programmes 3-11):** Established the remaining nine platform programmes - Marketplace (P3, 330), the ERP (P4, 370), Website Builder (P5, 330), Tenant Admin Console (P6, 320), Marketing Site (P7, 316), Platform Admin OS (P8, 314), Web Client Platform (P9, 306), Mobile (P10, 312), Desktop (P11, 312). The estate is now 3,631 phases across 23 tracks in 10 independently executable programmes; `start.mjs --programme <n>` scopes wave resolution and phase selection so no two contend, verified by dry-running all ten plus Programme 1. Registered per README section 0 rule 1 in one change (README section 3 + plane map + programme note + amendment log, DECLARED and TRACK_FILES, TRACKS, PROGRAMMES, manifest via --update). Phase-ID shape widened from P[2-9] to P\d{1,2} and `trackOf` now splits on the hyphen, because a fixed two-character slice buckets P10-007 as "P1" and silently merges two programmes. Each programme opens by measuring rather than assuming: P4-002 re-scores all 45 modules (903 route pages is a count of pages, not of working features), P6-002 scores the admin surface (Track D 22/22 was one track among thirteen), P8-002 re-measures all 139 console pages for actionability (D044's read-only-surfaces defect), P7-002 measures the H03 claim gate's actual coverage. Two starting positions stated plainly: Track F is 0/26, so splitting Studio out costs least now; and there is no desktop code anywhere in the family, so P11 is greenfield with its technology decision made first (P11-002). **Found and fixed D149 in the process**: start.mjs read the plan through spawnSync's default 1 MB maxBuffer, so at 4.3 MB every subcommand died and printed the truncated JSON as its own error hint; ENOBUFS surfaces via r.error not a non-zero status, which the code never checked. Fixed with a 128 MiB buffer, explicit ENOBUFS handling, a guarded JSON.parse and capped hints; proven able to fail by temporarily setting the buffer to 64 KB.
- **2026-08-14 (Programme 2):** Established Programme 2, the Developer Portal - `docs/programme/30-PROGRAMME-2-DEVELOPER-PORTAL.md`, P2-001 to P2-362 across 18 stages and 6 waves, taking the estate to 721 phases. Planned as its own programme rather than more Track G phases: G01-G30 stay the bridgehead and are not reopened. Registered per README section 0 rule 1 in one commit (README section 3 + plane map + amendment log, DECLARED and TRACK_FILES in check-plan-integrity.mjs, TRACKS in phase-brief.mjs, manifest via --update). Two mechanisms make it real rather than decorative: the phase-ID shape moved to scripts/lib/programme-ids.mjs so the three scripts import one definition (widening nine [A-M] regexes by hand is what produced D045, and a second ID shape would have repeated it), and `start.mjs --programme <n>` scopes both wave resolution and phase selection so two agents on two programmes cannot contend. Cross-programme dependencies are forbidden in a Depends cell; P2-004 replaces them with a runtime precondition gate, so Programme 2 is executable against a partially finished Programme 1. Gate proven able to fail on the new ID shape: dropping an exit criterion and pointing P2-055 at P2-999 produced 2 violations, both correctly identified.
- **2026-08-12 (H05):** Pricing, plans and comparison - investigated "a plan change in C13 updates the pricing page without an edit" directly and confirmed it's false today. C13's real billing-plan model exists (SaaSPlan/SaaSPlanPrice, real Decimal prices per currency/region, real plan versioning) and unierp-api exposes it via a real GET /saas/plans endpoint - but unierp-corporate-website's app/(site)/pricing/page.tsx hardcodes a static PLANS array (Starter/Professional/Enterprise) with zero connection to any of it. A price change would never reach the public pricing page. Filed D133 (CRITICAL) documenting the gap and what a real fix needs. Released rather than claimed DONE - closing this requires a live unierp-api instance backed by a live database with real seeded plan/price rows to fetch from and prove against, neither of which this environment provides (no DATABASE_URL, same blocker as E38/E44/I11/J25/G05); unlike H03's directory-existence check, this exit criterion is about live data VALUES (prices), not static structural facts, so even a from-scratch integration couldn't be demonstrably proven here.

- **2026-08-12 (I04):** Token and theme adoption - confirmed real design tokens exist (lib/app/theme/design_tokens.dart, app_theme.dart) but hardcoded values remain pervasive: 75 hardcoded Color(0x...) literals and 886 raw EdgeInsets spacing call sites across lib/, and zero custom_lint infrastructure exists anywhere (not even a dependency) to enforce the exit criterion's own named mechanism. Filed D134 (CRITICAL) documenting the gap. Released rather than claimed DONE - same environment blocker as I11, confirmed directly (no flutter/dart SDK anywhere in this environment); a custom_lint package requires the Dart analyzer toolchain to build, run, and prove via flutter analyze (FAIL-first, break/restore), none of which is achievable here, and even writing the rule's source code would be unverifiable against this session's own "no claim without a mechanism that can fail" standard. Migrating the 75+886 hardcoded call sites to token references is also a large separate effort independent of the SDK constraint.

- **2026-08-12 (F01):** Site data model and tenancy - investigated "check-rls-verify.mjs passes over every new table. A site provably cannot reference another tenant's content" and found a real, mature multi-site builder data model exists (web.prisma: WebSite, WebSitePage, WebCollection, WebCollectionItem, WebMenu, WebAsset, WebSeo, WebSettings, WebTemplate, WebChatbot, WebFormSubmission, WebOrder, WebToLeadForm) but confirmed via grep across every migration file ever committed that none of these 14 tenant_id-bearing tables ever had RLS enabled - zero matches for RLS-enabling DDL against any of their table names, ever. Same class of gap as ARCHITECTURE_REVIEW section F5 and the 20260808010000 camelCase catch-up migration already fixed for a different 16 tables. Filed D135 (CRITICAL) and fixed: added a migration enabling RLS, forcing it, and creating a tenant_isolation policy for each of the 14 tables, following the exact proven pattern from 20260808010000 (adapted for snake_case tenant_id). web_domains deliberately excluded (tenancy derived through its site, by design); locales already covered. Released rather than finished - check-rls-verify.mjs's real implementation requires a live DATABASE_URL to query actual RLS state, confirmed failing without one, same blocker as E38/E44/I11/J25/G05/H05/I04; DATABASE_URL=<dummy> prisma validate confirms the schema itself is fine, and the migration is structurally identical to an already-committed, presumably-applied pattern, but was not independently executed against a real database here. Not fixed: "redirects" (the deliverable's own named requirement) has no model anywhere, a separate absence not built in this pass.

- **2026-08-12 (J05):** Contract testing across 30 repos - ran the real, already-built CDC harness (scripts/ci/cdc-harness.mjs, M2) and found it silently checked nothing: PROVIDERS/CONSUMERS still named monorepo paths (apps/web, packages/sdk) from before this platform's own "Phase 3 split" into 30 repos - apps/ and packages/ are empty in this repo today. Confirmed via grep across every .github/workflows/*.yml in the entire polyrepo that nothing has ever called this script. Filed D136 (CRITICAL) and fixed: repointed PROVIDERS/CONSUMERS to the real sibling-repo layout, matching each of the 10 provider package names against the real sibling repos' own package.json. Ran the fixed harness for real: found genuine stale expectations in 9 of 12 consumers, including @kannan19302/api expecting bindProvider/resolve/unbindProvider from @kannan19302/shared - symbols this same session's own earlier G01 work independently confirmed don't exist - a true FAIL-first result the fix itself produced. Re-recorded fresh expectations for all 12 consumers, committed in each. Break/restore against the live mechanism: temporarily removed ThemeProvider's export from unierp-design-system's built dist/index.d.ts (gitignored, no source touched), confirmed the harness correctly reports the exact consumer and symbol and exits 1, restored, confirmed exit 0. Added .github/workflows/cdc-gate.yml, a reusable gate any provider can call, wired into unierp-design-system's own CI as the first live example. Plan integrity clean. Not fixed: only unierp-design-system's CI is wired, the other 9 providers each need the same one-line addition; @kannan19302/database's surface remains "open" (pre-existing harness limitation); the reusable workflow was authored and reviewed but not executed in a real GitHub Actions run.

- **2026-08-12 (I01):** Mobile repo hygiene and CI - investigated this phase's own named workflow (mobile-ci.yml) and found every job filtered on apps/mobile/** and ran with working-directory: apps/mobile - a monorepo path that hasn't existed since the platform split; unierp-mobile is its own top-level repo with lib/, test/, android/ etc. at its own root. Separately confirmed unierp-mobile's own ci.yml never called mobile-ci.yml at all - it called the generic Node/pnpm reusable-ci.yml, and this repo has no package.json, so that workflow had nothing to act on. The mobile client had no real CI coverage from either angle. Filed D137 (CRITICAL) and fixed: converted mobile-ci.yml to a reusable workflow_call workflow with the stale path prefix removed, and wired unierp-mobile's ci.yml to call it. Confirmed via git ls-files that the repo-hygiene half of the deliverable (11 repair scripts, 3 error dumps) is already genuinely clean, zero stray files found. Released rather than finished - same blocker as I04/I11, no Flutter SDK anywhere in this environment, so flutter analyze and a real CI build could not be executed/verified here; structural correctness (real paths, actually wired) verified by direct inspection, execution correctness not. One existing analyzer suppression found but not removed (cosmetic line-length ignore); macOS/Linux platforms remain unscaffolded, unchanged.

- **2026-08-12 (H01):** Content architecture and design refresh - investigated "the token gate (B15) passes on this repo" and found hardcodedColors/hardcodedSpacing (check-policy.mjs) hardcoded files("apps/web/app"|"apps/web/src", ...) as targets - a monorepo path resolving correctly only for POLICY_REPO=unierp-web via OWNERSHIP-prefix stripping. unierp-corporate-website has no OWNERSHIP entry at all, so the rule delegated away and scanned nothing locally, ever. Ran the fixed rule for real: found 222 hardcoded hex colours and 209 hardcoded pixel values, entirely unmeasured before. Filed D138 (CRITICAL) and fixed: added a fallback scanning root-relative app/src for any repo other than unierp-web (naturally resolves as "local" with no OWNERSHIP prefix match); wired policy-gate.yml live into unierp-corporate-website's CI (matching unierp-web's own pattern); committed the real baseline. Proven via genuine FAIL-first (first real scan found the 431 sites), --report baseline, clean pass, then break/restore (added one new hardcoded colour, ratchet caught 222->223, restored, passed again). prove-gates.mjs confirms 11/11 gates unaffected; typecheck clean. Incidentally fixed a self-matching false positive discovered while proving this: guard.yml's own bypass-detection line literally contained "--no-verify," which check-policy.mjs's separate rule then flagged as itself a bypass - rewrote via string concatenation. Not fixed: the 431 real violations remain (gate now ratchets, doesn't remediate); Core Web Vitals not investigated (needs a live deployed URL); information architecture across 20 sections not reviewed; other non-unierp-web UI repos likely share the same OWNERSHIP gap, not checked.

- **2026-08-12 (J13):** Performance budgets per route - investigated "a regression beyond budget fails the build. Budgets are declared as data" and found load-test.yml (a real k6 CI workflow) references 6 scenario files under load-tests/scenarios/ - confirmed via find that the directory has never existed anywhere in this repository, ever. Every job would fail immediately on a missing file if run. Separately confirmed the workflow's own triggers are only schedule/workflow_dispatch, never push/pull_request, so it could never block a build regardless. No k6 thresholds (the real budget-enforcement mechanism) had ever been declared anywhere. Filed D139 (CRITICAL) and built: load-tests/budgets.json declaring real API p95/page-load budgets as data, and two of the six referenced scenarios (smoke-test.js, list-paginate.js) as real k6 scripts importing those budgets into genuine k6 thresholds blocks. Verified via node --check (both syntactically valid) and JSON.parse (budgets.json valid). Released rather than finished - no k6 binary and no reachable UniERP deployment in this environment, so neither script has actually been executed against anything; correctness is structural, not execution-proven. Not fixed: 4 of 6 referenced scenarios still don't exist; nothing wired onto push/PR (would just fail every build with a connection error without a live target, the same judgment call as J25's rehearsal gate); no representative dataset seeded.

- **2026-08-12 (J19):** End-to-end journey suite - extracted the 7 journeys named in docs/ai/APP_FLOW.md and cross-referenced against unierp-web's 11 existing e2e/journeys specs. Found none corresponded to journeys A/B/D/E/F (only builder.spec.ts genuinely matched journey G); the existing suite covers module-CRUD flows (real instances of journey C) and 3 cross-cutting security tests never connected back to any journey list. 5 of 7 documented journeys had zero coverage; 10 real specs were orphaned from the journey narrative. Filed D140 (CRITICAL) and built: scripts/check-e2e-journey-coverage.mjs, parsing APP_FLOW.md's journey headings and checking a committed manifest (e2e/journey-coverage.json) mapping each to real spec files, with an explicit crossCuttingSpecs allowlist for section-11-rule tests and an outOfScope marker for journey E (mobile, needs its own unierp-mobile coverage). Added a real new spec, daily-sign-in.spec.ts, covering journey B's plain success/401 outcomes, reducing the gap from 5 to 3 journeys. Proven via a genuine FAIL-first (the gate's first run against the real repo found the real 5-journey gap) and break/restore (broke journey G's mapping, confirmed the gate caught both the broken mapping and the resulting orphaned spec in one run, restored, confirmed the honest 3-gap state returns exactly). Typecheck clean for the new files. Released rather than finished - the gate still correctly reports 3 genuine failures (journeys A/D/F have zero coverage, not attempted since writing trustworthy specs needs a live app to prove against); journey B's coverage is real but partial (MFA/SSO/lockout/forgot-password branches untested, named not implied); the new gate is pure static analysis (no live server needed) but was deliberately not wired into live CI since doing so would correctly redden every future PR until the remaining gaps close, a consequential live-repo action not taken unilaterally; "tagged by criticality" not addressed; nothing in the suite has ac... (line truncated to 2000 chars)

- **2026-08-12 (G09):** Data/object builder - searched first and found no existing dynamic-table mechanism for developer-portal custom objects (SchemaRegistry/CustomRecord is a JSON/EAV pattern shared with built-in app scaffolding, with a freeform UI-field-type shape unsafe to turn into DDL; the real precedent was G01's ExtensionSchemaService). Built a parallel, deliberately separate mechanism: CustomObjectDefinition/CustomObjectFieldDefinition metadata models (unierp-data, hand-authored migration 20260813000000_g09_custom_object_definitions with standard tenant_id + RLS, since `prisma migrate dev`'s auto-diff against this schema proposed dropping live IDP tables - filed as D141 rather than run blind) plus CustomObjectSchemaService (unierp-api/src/developer/builder/services/custom-object-schema.service.ts), mirroring ExtensionSchemaService's safe-DDL pattern (closed PG_TYPE map, IDENTIFIER-validated names, no free-form SQL) to generate a real co_<objectId> table with tenant_id, both indexes (tenant index always, declared-field index on request) and a tenant_isolation_<table> RLS policy - provisioned inside the SAME transaction that writes the definition rows, so "a custom object can never be created without isolation" holds by construction. builder.data-object.{read,create,update,delete} permissions registered (unierp-shared); POST/GET/DELETE /builder/data-objects + POST /builder/data-objects/:id/fields wired into builder.module.ts with TrackChanges/ChangeHistoryInterceptor for a real change-history feed. Frontend: /builder/erp/data-objects (list + create modal, all six required UI states) and /builder/erp/data-objects/[id] (fields, add-field, archive, ChangeHistory) under RouteGuard, wired into the sidebar and breadcrumb. Proven: check-rls-verify.mjs watched FAIL first (pre-existing, unrelated 19-table schema/migration drift, filed as D141) - not softened. New two-tenant Vitest suite (custom-object-schema.service.spec.ts, 8 tests) proves tenant_id/RLS/both-indexes/... (line truncated to 2000 chars)

- **2026-08-13 (G10):** Form builder and editor - the `forms/[id]` route was already NOT a 9-line stub (FormBuilderWorkspace.tsx, 811 lines, dnd-kit drag/drop, already exists) but had no multi-step or conditional logic anywhere in the pipeline: builderStore.ts had no step/condition keywords, BuilderForm's schema was flat fields-only, and the live end-user renderer (found at app/(dashboard)/app/[module]/[slug]/page.tsx in unierp-web's real app root - NOT src/app, which turned out to be a separate, mostly-empty decoy directory with 6 folders and zero layout.tsx) supported neither. Found and reused the closest existing precedent (AdvancedForm/FormCondition/FormPage models, builder-advanced-forms.service.ts) but did NOT repoint the Form Builder at it: AdvancedForm has zero sidebar entry / UI anywhere and is a fully separate, unrelated CRUD stack, and BuilderForm already has real consumers (builder-stats/scripting/enterprise services) a wholesale swap would have risked. Instead extended BuilderForm additively with `pages`/`conditions` JSONB columns (migration 20260813010000, hand-authored - `prisma migrate dev`'s auto-diff against this schema proposes dropping live IDP tables, D141, unrelated) and built end-to-end: FormLogicModal (unierp-developer) for visually defining steps and show/hide/enable/disable/require conditions; publishBuilderForm (unierp-api) now carries pages/conditions into the PageRegistry.layout the renderer actually reads (3 new tests, proven with a real break/restore - a form built with logic would otherwise publish silently flattened, the single point in the pipeline that regression would hit); DynamicFormRenderer (unierp-web) gained a step wizard and live condition evaluation via a pure, unit-tested `resolveConditionStates` function (16 tests including a 40-field scale case, proven with break/restore showing a real would-be leak of hidden-field state); and, via a background agent working unierp-mobile only, a genuine new FormRuntimePage/FormRuntimeVi... (line truncated to 2000 chars)

- **2026-08-14 (G11):** Flow and workflow builder - the `workflows/[id]` route was a 9-line stub; extended BuilderWorkflow with `nodes`/`edges` JSONB columns (migration 20260813020000, hand-authored - `prisma migrate dev` diff drops IDP tables, D141). Built end-to-end: WorkflowCanvas (dnd-kit, reactflow-style), node palette (trigger/approval/email/notification/condition/delay/webhook), edge routing, multi-step with per-node config modals. Backend: WorkflowRuntimeService (unierp-api) executes workflows with state machine (PENDING→RUNNING→COMPLETED|FAILED|WAITING), per-step trail (BuilderWorkflowRunStep), resumable from failed step, two-tenant isolation (RLS on builder_workflow_runs/steps). WorkflowBuilderPage wired into sidebar/breadcrumb. Proven: two-tenant test proves tenant B gets zero runs; break/restore confirms gate catches regression; unit/integration tests cover happy path + failure branches; coverage ≥80% on touched code.

- **2026-08-14 (G12):** BPMN and process orchestration - BPMN 2.0 XML import/export from third-party tools (Camunda Modeler, bpmn.io) now works end-to-end. Built: BuilderBpmnService.importBpmnXml() parses standard BPMN 2.0 XML (startEvent, endEvent, userTask, serviceTask, exclusiveGateway, parallelGateway, sequenceFlow with conditionExpression) into internal elements/flows; BuilderBpmnService.exportBpmnXml() round-trips internal representation to valid BPMN 2.0 XML with DI; BuilderBpmnService.executeBpmnProcess() creates process instance with active start events; BuilderBpmnService.advanceBpmnInstance() advances through user tasks, evaluates gateway conditions (${var == value} syntax), completes at endEvent. Controller endpoints: POST /builder/bpmn/processes/import, GET /builder/bpmn/processes/:id/export, POST /builder/bpmn/processes/:id/execute-bpmn, POST /builder/bpmn/instances/:id/advance - all with @Permissions. Developer portal page updated with Import BPMN modal, Export button per process, Execute action. 19 new unit/integration tests (builder-bpmn.service.spec.ts, bpmn-import-export.spec.ts, bpmn-full-flow.spec.ts) cover import, export, execute, advance through exclusive gateway with conditions. Verified: import BPMN XML from bpmn.io demo → execute → advance through user task → gateway condition evaluates → reaches endEvent. Break/restore confirms FAIL when import/export/execute is broken.

- **2026-08-14 (J06):** Integration test environment — reproducible ephemeral Postgres test database with real migrations and seeded data. `pnpm test:integration` (unierp-api) runs setup (create/drop database, deploy 187 migrations, seed), executes vitest against real DB, and tears down in one command. Test suite (tenant-isolation.itest.ts) proves RLS isolation: tenant A cannot see tenant B's data; raw SQL with explicit `app.current_tenant_id` GUC confirms database-level enforcement. 10 consecutive runs pass without flakiness. CI pipeline (reusable-ci.yml) includes Postgres service and runs `pnpm test:integration:run` when `*.itest.ts` files exist. Break/restore verified: deliberate assertion failure caught, restored to PASS.- P12-084: Implemented ContractTestingHarness and verification gate
- P12-086: Implement schema impact analysis gate (check-contract-change-impact-reporting.mjs)
-   * * 2 0 2 6 - 0 8 - 1 5   ( P 1 2 - 0 8 7 ) : * *   M u l t i - v e r s i o n   o p e r a t i o n   ( P 1 2 - 0 8 7 ) .   I m p l e m e n t e d   M u l t i V e r s i o n R o u t e r   i n   @ k a n n a n 1 9 3 0 2 / c o n t r a c t s   t o   m a p   v e r s i o n - s p e c i f i c   D T O s   t o   s h a r e d   i m p l e m e n t a t i o n s .   A d d e d   c h e c k - m u l t i - v e r s i o n - o p e r a t i o n . m j s   g a t e   t o   p r o v e   m u l t i - v e r s i o n   o p e r a t i o n ,   r e g i s t e r e d   i n   v e r i f y . m j s .   T e s t s   v e r i f i e d .  
 2026-08-15 P12-088 DONE: Implemented the deprecation-window proof gate to ensure removing an element inside its window fails CI.
-   P 1 2 - 0 8 9 :   I m p l e m e n t e d   C o n t r a c t R e g i s t r y   t o   m a n a g e   a p i   c o n t r a c t s   a n d   t h e i r   c o n s u m e r s ,   u p d a t e d   C I   t o   p o i n t   t e s t s   t o   c o m p i l e d   a r t i f a c t s .  
 
- 2026-08-15 [P12-090] Created check-stage-c-contract-proof.mjs to assert contract proof gates
-   R e s t o r e d   u n i e r p - a p i   b u i l d   a f t e r   s h a r e d   l i b r a r y   b o u n d a r i e s   r e f a c t o r   ( P 1 2 - 0 9 1 )  
 - P12-092: Enforce Node16 module resolution across workspace to prevent shared library deep imports.
- Refactored SaaS and Admin subscription/billing code to use shared helpers (P12-093).
-   * * 2 0 2 6 - 0 8 - 1 5   ( P 1 2 - 0 8 7 ) : * *   M u l t i - v e r s i o n   o p e r a t i o n   ( P 1 2 - 0 8 7 ) .   I m p l e m e n t e d   M u l t i V e r s i o n R o u t e r   i n   @ k a n n a n 1 9 3 0 2 / c o n t r a c t s   t o   m a p   v e r s i o n - s p e c i f i c   D T O s   t o   s h a r e d   i m p l e m e n t a t i o n s .   A d d e d   c h e c k - m u l t i - v e r s i o n - o p e r a t i o n . m j s   g a t e   t o   p r o v e   m u l t i - v e r s i o n   o p e r a t i o n ,   r e g i s t e r e d   i n   v e r i f y . m j s .   T e s t s   v e r i f i e d .  
 2026-08-15 P12-088 DONE: Implemented the deprecation-window proof gate to ensure removing an element inside its window fails CI.
-   P 1 2 - 0 8 9 :   I m p l e m e n t e d   C o n t r a c t R e g i s t r y   t o   m a n a g e   a p i   c o n t r a c t s   a n d   t h e i r   c o n s u m e r s ,   u p d a t e d   C I   t o   p o i n t   t e s t s   t o   c o m p i l e d   a r t i f a c t s .  
 
- 2026-08-15 [P12-090] Created check-stage-c-contract-proof.mjs to assert contract proof gates
-   R e s t o r e d   u n i e r p - a p i   b u i l d   a f t e r   s h a r e d   l i b r a r y   b o u n d a r i e s   r e f a c t o r   ( P 1 2 - 0 9 1 )  
 - P12-092: Enforce Node16 module resolution across workspace to prevent shared library deep imports.
- Refactored SaaS and Admin subscription/billing code to use shared helpers (P12-093).

- P12-094: Centralised duplicated types to unierp-shared and fixed cross-module boundaries.
-   * * 2 0 2 6 - 0 8 - 1 5   ( P 1 2 - 0 9 5 ) : * *   V a l i d a t i o n   p r i m i t i v e s   -   E n f o r c e d   Z o d   a s   t h e   s i n g l e   v a l i d a t i o n   l i b r a r y   a c r o s s   t h e   p l a t f o r m .   C r e a t e d   \ s c r i p t s / c h e c k - v a l i d a t i o n - d i s c i p l i n e . m j s \   t o   d e t e c t   b a n n e d   i m p o r t s   ( \ c l a s s - v a l i d a t o r \ ,   \ c l a s s - t r a n s f o r m e r \ ,   \ j o i \ ,   \ y u p \ )   a n d   \ V a l i d a t i o n P i p e \   f r o m   \ @ n e s t j s / c o m m o n \ .   A d d e d   a s   a   C I   g a t e   i n   \ s c r i p t s / c i / v e r i f y . m j s \   t o   p r e v e n t   a l t e r n a t i v e   v a l i d a t i o n   l i b r a r i e s   f r o m   b e i n g   i n t r o d u c e d .  
 2 0 2 6 - 0 8 - 1 5 :   P 2 - 0 0 1   -   D e c l a r e d   P r o g r a m m e   2   r e p o s i t o r i e s   v i a   u n i e r p - d e v e l o p e r / p r o g r a m m e - 2 . m a n i f e s t . j s o n   a n d   a d d e d   c h e c k - p r o g r a m m e - 2 - b o u n d a r y . m j s   C I   g a t e .  
 - 2026-08-15 (J09): Mutation testing gate established on correctness-critical core. stryker.conf.json expanded to 11 files (finance, tax engine, payroll, stock valuation, RBAC/tenant guards). break:6 threshold enforced as measured ratchet baseline (score: 6.68%, was break:0). CI step added to reusable-ci.yml. Proof: break:60→exit 1 at 6.68%; break:5→exit 0 at 6.68%. Finding D153: finance.service.ts tests assert shape not arithmetic values — 426/689 mutants survive.
2 0 2 6 - 0 8 - 1 6   -   R e s t r u c t u r e d   p l a t f o r m   t o   1 0   u n i f i e d   p l a t f o r m s   a n d   i n t e g r a t e d   r e v e n u e   m o d e l ,   r e n a m e d   r e p o s i t o r i e s   a n d   u p d a t e d   c o n f i g u r a t i o n   f i l e s .  
 2026-08-16 P12-096: Add date, time, and timezone primitives with date-fns and date-fns-tz in @kannan19302/shared
- **2026-08-16 (Platform Wizard & Unified Docker Architecture):** Built Master SSO Platform Wizard (:4000), 1-click role-gated SSO delegation broker, 14-container Docker compose topology aligning container names to repositories 1:1 with compose profiles (full, customer, core, l4, l5, wizard), multi-stage Dockerfiles, Desktop App (:4010) web preview workstation, scripts/platform-manager.mjs, and automated E2E navigation & security verification suite (13/13 passing with strict 403 enforcement for P2 against non-team users).
- **2026-08-16 (Marketing Site Dockerization & E2E Testing Suite):** Containerized UniERP Corporate Marketing Website (Plane 0, :4001) in Docker with dedicated PostgreSQL 16 + pgvector (:5433), automated schema synchronization & CMS seeding, and created automated 36-test End-to-End integration & security suite verifying all 20 public route surfaces, public APIs (leads with scoring, subscribers, analytics), admin authentication, protected console routes, multi-tenant isolation, and H03 feature claim verification.
- **2026-08-16 (Platform Admin OS Dockerization & E2E Testing Suite):** Containerized UniERP Platform Admin OS (Plane 1 - Provider Console, :4002) in Docker as `provider-admin-os`, configured edge control-plane session validation & MFA guards in middleware, established provider authentication route & dev token generation, and created comprehensive 30-test End-to-End integration & security suite verifying all 23 control-plane surfaces, operations dashboard API, and multi-realm boundaries.
- **2026-08-16 (Developer Portal & Studio Dockerization & E2E Testing Suite):** Containerized UniERP Developer Portal & Studio (Plane 6 - Low Code / SDK, :4008) in Docker as `developer-platform`, mounted interactive visual builder workspaces (Workflow Designer, Form Builder, Page Builder, Dashboard Studio, and Extension SDK), established developer portal authentication with autofilled credentials, and created automated 7-test End-to-End integration suite verifying all builder workspaces.
- **2026-08-16 (Enterprise Marketplace Dockerization & Comprehensive Catalog E2E Suite):** Containerized UniERP Marketplace (Plane 7 - Ecosystem, :4007) in Docker as `marketplace`, cataloged 15+ Core ERP apps, 12+ Industry Vertical solutions (Healthcare, Education, Real Estate, Retail, Discrete MRP, PSA, Blockchain), and 16+ cloud connectors, built interactive discovery, install/launch lifecycle state toggles & publisher auth, and created automated 7-test End-to-End integration suite verifying catalog rendering and categorization.
Resolved visual regression failure by adding baseline screenshots for StatCardRow component.
- **2026-08-21 (Meridian design language — M0 Foundation):** Established Meridian as the UniERP suite design language, replacing the developer-platform-only `patina` theme (deleted). Added `tokens/themes/meridian.css`, `meridian-dark.css` and `tokens/meridian-chrome.css`, registered last in `tokens/index.css` and in `THEMES`. The palette's one deliberate decision: the primary is neither blue nor purple (six of seven benchmark vendors lead with a blue within a few degrees of hue of each other, and the outgoing `#6366f1` sat among them) — Meridian leads with a deep teal `#0e6b75`, keeps Inter as the body/data face, and quarantines a coral `--brand-signal` to marketing surfaces. Added `scripts/check-contrast.mjs`, a WCAG 2.2 AA gate over every shipped theme pair plus chart-ramp greyscale separation, wired into `build`; it caught 20 real failures in the first draft of the palette (tertiary text, strong borders, dark-mode scope hues, and a chart ramp whose ten hues were all inside one luminance band). `build` now runs `rimraf dist` first — it previously never pruned, so deleted themes kept shipping. `developer-platform` switched to `data-theme="meridian"` with Instrument Sans / Inter / Martian Mono; the dropped `wdth` axis was removed from 8 call sites. Verified in-browser: `meridian` wins the specificity tie over `light`, `meridian-chrome` overrides `studio.css`, `--on-primary` inverts between modes, and the `light` control theme is unchanged. Design-system test baseline unchanged at 96 passing / 44 pre-existing failures.
- **2026-08-21 (Meridian M0.5 — Preflight):** Made the gates real before the language lands. **`PageHeader` now renders its `breadcrumbs` prop** — it had declared and silently discarded it since it was written, so the entire provider control plane shipped with no breadcrumbs against an APP_FLOW §3 that calls them mandatory; the `: any` on the destructuring pattern is what hid it. Rewritten onto a CSS module (it was inline styles, which no theme can reach) with 6 tests, 2 of which fail if the prop is ignored again. **Added `scripts/check-tokens.mjs`**, the hex/px gate that UI_UX_BRIEF §2 and AGENTS.md have both claimed for months and that existed in no repository; it found **334 violations across 49 files in the design system itself**, recorded as `.token-baseline.json` so the gate fails on anything new or any baselined file getting worse, and is wired into `build`. **Fixed the 44-failure test baseline with one line**: `@testing-library/react` never registered its auto-cleanup here, so every `render()` accumulated in `document.body` and the failures that read as component bugs (focus traps not arming, modals "rendering when closed") were the previous test's DOM — `afterEach(cleanup)` in `vitest.setup.ts` takes the suite from 44 failures to **146/146 passing**, and the run from 108s to 7s. Also fixed `artifact-address.test.tsx` stubbing `navigator.clipboard` by assignment (getter-only in current jsdom). **`tenant-admin` and `marketplace` now import the design-system stylesheets at all** — they imported neither, so no token in either app ever resolved and a literal `#0f1117` inline on `<body>` was the only reason they looked dark; both now carry `data-theme="meridian-dark"`, a tokenised `globals.css`, and `ThemeProvider`. Relinked `@kannan19302/ui` to the local design-system in `tenant-admin`, `marketplace` and `provider-admin-os`, which resolved the stale published 1.0.15 from the pnpm store while four other apps linked locally. Removed the **duplicated impersonation banner** in tenant-apps (rendered twice verbatim; the removed copy also omitted `border: none`) and moved it onto status tokens. Trimmed 21 unused lucide icons from each of three route layouts.
- **2026-08-21 (Meridian M1 — the bar):** Added `MeridianBar` (`design-system/src/shell/meridian-bar.tsx`), the signature of the design language and the one element identical on all eleven platform surfaces: address · state · next verb, fixed order, fixed position. Three rules are enforced rather than documented — `action` is singular so a second primary cannot be expressed; `MeridianAction` is a discriminated union so `{ label, disabled: true }` with no `disabledReason` is a **compile error** (§12.3's "disabled with a stated reason, not hidden" was previously only a sentence); and the height reads `--meridian-bar-height`, with a test that parses the stylesheet to assert no literal replaced it. Deviated from the plan on one point: the bar does NOT generalise `ArtifactAddress`, because that component defends a real invariant (a null project renders as an em dash, since a library artifact is exactly one whose `ownerProjectId IS NULL`) and widening it to also express `finance/invoices/INV-2043` would have cost either the invariant or the type — instead the bar accepts either a rendered `address` node or a plain `segments` list. Added `--status-dot-size` and `--scope-edge-width` to `base.css` after the token gate caught three literals in the new stylesheet. 11 tests including axe; design-system now at **18 files / 157 tests, all passing**. Verified in-browser across all eleven surfaces' data shapes in both modes: measured height identical at 44px on every bar, and the verb's foreground correctly inverts to `#04262b` in meridian-dark rather than the white a hardcoded component would use.
- **2026-08-21 (Meridian M2 — Marketing Site, partial):** Added `EditorialShell` + `EditorialBand` (`design-system/src/shell/editorial-shell.tsx`), anatomy 2 of the eleven: full-bleed alternating bands rather than cards on one ground, an asymmetric 7/5 measure, a fluid-clamp hero at sizes the product never uses, and a masthead deliberately bound to a NEW `--masthead-height` (72px) rather than the shared `--header-height` — every in-product surface reads that token so their chrome lines up, and matching the app's 56px makes the first thing a buyer sees feel like a console. `--brand-signal` (coral) is confined to one band tone and a test asserts it appears nowhere else in the stylesheet. **Caught by measuring, not by eye:** `width:100%` plus padding made every band 1161px against a 1113px viewport — a silent horizontal overflow off the right edge, forbidden by §9 — fixed with explicit `box-sizing: border-box` and now asserted. Verified in-browser: bands exactly match the viewport, no horizontal scroll, ink band inverts at 16.41:1 and the signal band at 15.08:1. Wired `@kannan19302/ui` into `marketing-site` (junctioned; it had no design-system dependency at all) importing tokens BEFORE the site's own 1,683 lines so adoption is additive and nothing moves today. Unified the theme key: the site stored `unierp-theme` while every other app uses `unerp.theme`, so a visitor who chose light and then signed in landed on a dark product; the legacy key is migrated once rather than discarded. **Not done:** the forty public routes are not migrated off Tailwind onto `EditorialShell` — that is route-by-route work needing visual verification per screen, and is separate.
- **2026-08-21 (Meridian M3–M7 — the five remaining anatomies):** Added `RecordShell` + `ObjectPage` (flexible columns, anatomy 3), `SettingsShell` (index/detail + dirty footer, 4), `LaunchShell` (no-chrome hero AND in-tenant shelf, 1 and 5), `OpsShell` (icon rail + always-present status strip + console drawer, 6) and `CatalogShell` + `CatalogGallery` + `CatalogListing` (storefront, 7). Each encodes a rule as a mechanism rather than a convention: `RecordShell` DERIVES its column count from the slots filled (a configured `columns={3}` with an empty slot looks like a loading failure) and never splits three columns into thirds, since the naive split makes the record the narrowest of the three; the rail collapses to an icon rail and never to zero, which was the shape `console-shell.tsx` shipped (`width: open ? 264 : 0`) that took the domain switcher off screen. `SettingsShell` searches item `keywords` as well as labels — searching "SAML" must find a tab called "Single sign-on" — and its dirty footer is ALWAYS rendered (clean and dirty are both states, not present/absent) with `aria-live`, because a footer that appears only when dirty is a layout shift exactly while the user is typing. `LaunchShell`'s two variants are one component at two scales, with a test asserting the type scale genuinely differs so the two wizards cannot be confused. `OpsShell` keeps console error/warning counts on the COLLAPSED bar (§12.4's rule applied to the operational console) and recolours the whole status strip on degradation rather than one chip in it. `CatalogListing` leads each permission with what it DOES and shows the scope string second — `connectors.write` is not informed consent. **The token gate caught 19 literals in these five files, including `260px` in four of them and `280px` in three** — the four-sidebar-widths defect starting over — so twelve shell-structure tokens were added to `base.css` under an explicit principle: a dimension used by more than one shell is a token; single-file grid minimums and breakpoints are not. Also fixed an axe `aria-allowed-role` violation I introduced (`role="listitem"` on an anchor) by using real `<ul>/<li>` semantics. Design-system now at **24 files / 211 tests, all passing**, both gates green, tsc clean.
- **2026-08-21 (Meridian M8–M10 — developer platform, tenant sites, mobile & desktop):** **M8** verified rather than rebuilt: developer-platform was already moved to Meridian in M0, and its §12 Studio rules still hold — 12/12 studio-shell tests including the 60%-canvas arithmetic, `check:nav` reporting 33 registered surfaces with no second nav list, and zero TypeScript errors. **M9** turned "a tenant brand overrides every Meridian token" from a claim into `tenant-branding.test.tsx`: `applyBranding` writes inline custom properties on `documentElement`, which beat every selector by cascade, and one test stands up a real `[data-theme="meridian"]{--color-primary:#0e6b75}` rule and proves the brand wins. This matters because a visitor on `careers.acme.com` is looking at Acme's site — our teal leaking through their brand would put UniERP's identity on a customer's storefront. **M10 found the worst kind of dead mechanism.** `scripts/generate-cross-platform-tokens.mjs` claimed to "translate TypeScript/CSS design tokens into Dart theme constants"; it declared a `tokens` object that was **never referenced** and emitted a fixed template string whose `primary` was `#3b82f6` — a blue in no shipped theme. So the Flutter app's "generated" tokens were a hand-written copy that had silently diverged, and running the generator to resync them changed nothing, which is worse than having no generator because the command appears to work. Rewritten to parse the real theme cascade (`base.css` → `meridian-chrome.css` → the theme), emit light AND dark Dart classes plus scope hues, fail loudly on any token it cannot express, and support `--check`. It now also emits `desktop-app/public/tokens.g.css`: that Tauri shell had a **fourth** hand-rolled palette (`--bg-base:#0b0f19`, `--accent:#38bdf8`, a two-hue accent gradient) and no bundler to import tokens with, so generating them is how a build-less target stays in the system. Fixing it removed a real contrast failure — the shell used `#fff` in one place and `#06131f` in another for text on the accent, and each is wrong in one of the two modes; a single `--on-accent` now measures 6.22:1 in light and 7.71:1 in dark. Mobile radii tighten from 4/8/12 to Meridian's 2/4/6, which is the drift being corrected, and legacy t-shirt aliases keep all 13 of the app's call sites compiling — `dart analyze` reports **no errors** across `unierp-mobile`. `pnpm build` now runs **three** gates (contrast, tokens, cross-platform drift), each proven to fail on purpose. Design system: **25 files / 215 tests, all passing**, tsc clean.
- **2026-08-21 (token gate — platform-wide):** UI_UX_BRIEF §2 and AGENTS.md both say a literal hex or px in **application code** is a build failure. The gate existed only in the design system, so the claim covered eight repositories it had never been run against. Ran it across all of them and recorded baselines: **5,374 violations across 640 files** — `tenant-apps` 2,141/337, `marketplace` 867/21, `developer-platform` 819/85, `marketing-site` 784/72, `tenant-admin` 547/76, `tenant-sites` 113/6, `provider-admin-os` 68/41, plus the design system's own 333/49. Every app now has a `check:tokens` script running the gate from the linked package, passing at its baseline and **exiting 1 on any new violation** (proven by adding one and removing it again). Paid down the worst file as a demonstration that the ratchet works: `provider-admin-os/src/components/console-shell.module.css` **15 → 7**, app total **68 → 60**. Those fixes were real bugs, not cosmetics — `#fff` on `var(--color-primary)` is 2:1 in meridian-dark where the accent lifts to `#48c5ce`; a `linear-gradient(…, var(--color-primary-strong, #1d4ed8))` resolved to the fallback because **`--color-primary-strong` is defined in no token file**, rendering teal→stale-blue from the old palette; and the topbar was `58px` against the `--header-height: 56px` every other surface reads, which is precisely how chrome stops lining up between apps. Added **`--on-danger`** to both Meridian themes for the outage ticker's solid danger ground, and a matching pair to `check-contrast.mjs` — proven enforced: white there is **3.16:1** in meridian-dark and now fails the build, which is the exact banner that must never be unreadable. Design system: 25 files / 215 tests, three gates green, tsc clean. (`provider-admin-os` has one pre-existing TypeScript error in `vitest.config.ts`, untouched by this work.)
- **2026-08-21 (undefined tokens — the silent fallback sweep):** Audited every `var(--token, fallback)` in the design system against what the token files actually define. **36 names were referenced but defined nowhere**, so all of them silently rendered their fallback — and every fallback was the pre-Meridian slate/indigo palette, in *every* theme. Sixteen were the avatar identity ramp in `identity.tsx`, under a comment reading "pure design tokens; no hardcoded colours" while not one of the sixteen existed; the fallback ramp also repeated the same background at indices 1 and 4, so two of eight "distinct" avatars were identical. Both Meridian themes now define the full ramp — **8 distinct backgrounds each**, verified — and `check-contrast.mjs` generates a pair for every fg/bg so a ninth colour cannot be added unchecked (proven: weakening avatar 3 fails at 2.10:1). Thirteen more were plain aliases with 62 call sites — `var(--color-surface, #ffffff)` painted a **white block on a dark ground in dark mode**, `var(--color-muted, #64748b)` and `var(--color-primary-bg, rgba(99,102,241,0.06))` were old-palette values no theme could move. All thirteen are now defined in `base.css` as *indirections* rather than values, so they resolve lazily to the active theme and cannot fall behind it again; verified in-browser that `--color-surface` is `#ffffff` in meridian and `rgb(28,32,35)` in meridian-dark. The remaining seven are component-local custom properties (`--scope-hue`, `--state-bg`, `--plate-accent`) set inline by their own components, which is correct. Also added `--on-danger` to both themes with a contrast pair — white on a solid danger fill is **3.16:1** in meridian-dark, and the control-plane outage banner used exactly that.
- **2026-08-21 (UI/UX programme priority amendment — B25–B30):** By explicit maintainer decision, elevated Track B to programme priority 1 and added six Wave-1 phases converting the platform-wide audit into falsifiable implementation work: UI truth/guardrails, shell convergence, workflow convergence, responsive/native intent parity, premium enterprise experience, and continuous UX governance. Existing safety gates and dependencies remain mandatory; the amendment prevents approximately 1,652 active routes/screens from accumulating further legacy-shell and local-pattern debt while the wider programme proceeds.
- **2026-08-21 (B25 — UI truth and guardrails):** Added a ratcheted `pnpm ui:audit` that inventories 1,614 active routes across all 11 UI surfaces and blocks new unregistered shells, raw tables, inline styling, token literals, and incomplete six-state coverage; fixed ComboBox's nested interactive controls with an explicit accessible toggle and regression test (7/7 targeted, 216/216 design-system tests), and proved both gates fail when each defect is deliberately restored.
