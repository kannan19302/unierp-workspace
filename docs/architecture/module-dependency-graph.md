# Module dependency graph

Generated by `node scripts/generate-architecture-maps.mjs` from real
`from "../other-module/..."` relative imports found in
`unierp-api/src/modules/**`. A module importing another module's files
directly bypasses the outbox/event pattern this platform requires for
cross-module effects — every edge below is a real, direct coupling, worth
auditing, not just a graph for its own sake.

**4 direct cross-module import edge(s) found across 46 modules.**

| From | To |
| --- | --- |
| advanced-finance | finance |
| blockchain | outbox |
| ecommerce | outbox |
| sales | outbox |

