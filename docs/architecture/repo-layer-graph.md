# Repo layer graph

Generated by `node scripts/generate-architecture-maps.mjs` from the real
`dependencies`/`devDependencies` of every repo's own `package.json` in this
checkout, filtered to `@kannan19302/*` packages — the actual declared
repo-to-repo dependency graph, not asserted from memory.

**2 repo(s) with a package.json found.**

| Repo | Depends on (@kannan19302/*) |
| --- | --- |
| unierp-contracts | _none_ |
| unierp-workspace | _none_ |

