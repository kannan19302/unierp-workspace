# L10 — 1,000-line ceiling: exemption list

Generated by `node scripts/check-1000-line-ceiling.mjs --write-exemptions`.
Every non-test `.ts` file under `unierp-api/src` exceeding 1,000 lines, with
an individual, file-specific reason it is not yet under the ceiling.

**55 file(s) over 1,000 lines** (14 generated, excluded from
the "non-generated" count the exit criterion names; **41 non-generated**
file(s) require the justification below).

## Generated files (excluded by the exit criterion's own wording)

- `src/modules/blockchain/blockchain-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/drive/drive-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/ext-gateway/ext-gateway-generated.controller.ts` (1753 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/fixed-assets/fixed-assets-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/localization/localization-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/notifications/notifications-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/outbox/outbox-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/people/people-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/pwa/pwa-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/reporting/reporting-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/saved-views/saved-views-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/search/search-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/service-management/service-management-generated.controller.ts` (2122 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.
- `src/modules/subscriptions/subscriptions-generated.controller.ts` (1998 lines) — matches the platform's own `*-generated.*.ts` naming convention for machine-generated scaffolding.

## Non-generated files — individually justified exemptions

### `src/modules/advanced-finance/advanced-finance.controller.ts` (8283 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 8283 lines, 4 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/inventory/inventory.service.ts` (3990 lines, service)

Tracked by L08's service-decomposition gate (baseline: 3990 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/crm/crm.controller.ts` (2980 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 2980 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/finance/finance-operations.service.ts` (2956 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2956 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/communication/communication.service.ts` (2935 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2935 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/developer/builder/builder.service.ts` (2823 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2823 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/crm/crm-forecasting.service.ts` (2735 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2735 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-finance/finance-expansion-deep.controller.ts` (2730 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 2730 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/finance/finance-expansion.service.ts` (2558 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2558 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/hr/hr.service.ts` (2279 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2279 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/inventory/inventory.controller.ts` (2245 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 2245 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/hr-advanced/hr-advanced.service.ts` (2212 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2212 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-hr/advanced-hr.service.ts` (2183 lines, service)

Tracked by L08's service-decomposition gate (baseline: 2183 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/finance/finance-operations.controller.ts` (1983 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1983 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/pos/pos.service.ts` (1981 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1981 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/developer/builder/builder.controller.ts` (1836 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1836 lines, 2 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/marketplace/marketplace.service.ts` (1763 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1763 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/communication/communication.controller.ts` (1594 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1594 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/manufacturing/manufacturing-enterprise.service.ts` (1583 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1583 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/hr/hr.controller.ts` (1567 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1567 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/manufacturing/manufacturing.service.ts` (1527 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1527 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/procurement/procurement.service.ts` (1451 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1451 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-finance/services/financial-reporting.service.ts` (1449 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1449 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/crm/crm.service.ts` (1427 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1427 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/hr-advanced/hr-advanced.controller.ts` (1397 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1397 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/crm/crm-deals.service.ts` (1390 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1390 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/sales/sales-advanced-enterprise-core-deep.controller.ts` (1358 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1358 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/sales/sales-enterprise.service.ts` (1293 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1293 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-finance/services/gl-accounting.service.ts` (1249 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1249 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/sales/sales.service.ts` (1227 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1227 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/supply-chain/supply-chain-enterprise.service.ts` (1179 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1179 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-hr/advanced-hr.controller.ts` (1174 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1174 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/procurement/procurement-enterprise.service.ts` (1149 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1149 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/projects/projects-enterprise.service.ts` (1145 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1145 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-finance/services/tax-engine.service.ts` (1127 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1127 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/crm/crm-account-management.service.ts` (1111 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1111 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/advanced-finance/finance-more-deep.controller.ts` (1096 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1096 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/finance/finance-expansion.controller.ts` (1095 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1095 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.

### `src/modules/analytics/analytics-enterprise.service.ts` (1094 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1094 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/hr-advanced/hr-compensation.service.ts` (1075 lines, service)

Tracked by L08's service-decomposition gate (baseline: 1075 lines). Filed as part of D078 — the exit criterion for L08 explicitly rejects a split that only moves lines, so each remaining file needs its own read for a genuine responsibility seam before splitting; not completed for every violator in this pass.

### `src/modules/sales/sales-enterprise-execution-deep.controller.ts` (1024 lines, controller)

Tracked by L07's controller-decomposition gate (baseline: 1024 lines, 0 if-blocks). Filed as part of D077 — decomposing a controller this size safely requires moving real business logic to a service with proof of behavior preservation, a per-file undertaking not completed for every violator in this pass.
