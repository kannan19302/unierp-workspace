A18 — Sandbox escape-attempt suite
Exit criterion (verbatim): "Suite passes; each test fails when its mitigation is removed (proving the test tests something). Wired as a blocking CI gate."

PROOF 1 — suite passes:
  Command: npx vitest run src/escape-suite.spec.ts  (also: npm run test:escape)
  Output:  38 passed | 1 passed (test files), duration ~4.8s
  Each of the 38 tests = 19 threats x (contains-the-escape + escape-succeeds-under-mutation).
  The mutation-proving tests copy src/index.ts, apply a source mutation that removes exactly
  one threat's mitigation, load it in a fresh module registry (unique path per mutation so
  vitest never serves a stale transform), and assert the SAME adversarial payload now escapes.

PROOF 2 — a mitigation removed from the real source breaks the suite (suite can still fail):
  Mutation applied to real src/index.ts:  if (byteLength(argsJson) > hardening.bridgeBytes) {  ->  if (false) {
  Command: npx vitest run src/escape-suite.spec.ts
  Output:  2 failed (T11 contains + T11 proves) | 36 passed   -> FAILS as expected.
  Source then restored (git checkout -- src/index.ts) and suite re-run: 38 passed.

BREAK-BY-PROBE (each escape-succeeds test individually):
  T08a probe (mutated, timeout 2000ms vs 300ms loop): RESULT returned (loop completed) -> test passes.
  T07 probe (mutated, resolver injected, unapproved host): httpFetch called 1 -> escaped.
  T04 probe (mutated, memoryLimit 512): "Script execution timed out." -> escaped predicate true.

BLOCKING CI GATE:
  .github/workflows/ci.yml adds step "Escape suite (A18)" running `npm run test:escape`
  with NO `if: hashFiles(...)` guard (D013 anti-pattern) - it is a hard required step.

Full repo green: vitest 100/100 across 3 files; npx tsc --noEmit exit 0.
