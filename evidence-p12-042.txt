PHASE   P12-042
STATUS  DONE
PROVEN  node scripts/check-schema-size.mjs

PASS (normal run):
  ✅ Schema size gate passed: all 42 schema files are under 3000 lines (1908 total models).
  Exit code: 0
  Largest file: core-part-10.prisma at 2,309 lines.

FAIL (deliberate break — added __test_oversized__.prisma with 3,001 lines):
  ❌ SCHEMA FILE SIZE EXCEEDED (Max 3000 lines)
     - __test_oversized__.prisma: 3001 lines (exceeds 3000)
  No .prisma file may exceed 3,000 lines (Remediation R2 / Phase A04).
  Exit code: 1

Test file removed. Gate confirmed to fail when the condition it enforces is violated.

BUILT   unierp-workspace:
  - scripts/ci/verify.mjs — added Schema decomposition gate entry (P12-042)
  - .github/workflows/reusable-ci.yml — removed D013 hashFiles guard from integration tests step
  - docs/programme/breaking-changes-registry.json — added required policy + classifications metadata
  - scripts/check-versioning-policy.mjs — fixed ReferenceError (args and checkBumpIdx undefined in CLI block)
  - docs/test-taxonomy.json — regenerated to classify unierp-kernel/src/test-harness.spec.ts
  - docs/ai/CHANGELOG.md — one line appended

DoD:
  [x] Exit criterion passes; confirmed failing on deliberate break.
  [N/A] No new tables — this phase adds no database tables.
  [N/A] No new endpoints — this phase is infrastructure/tooling only.
  [N/A] No money-adjacent code.
  [N/A] No UI changes.
  [x] check-schema-size.mjs runs and enforces 3,000-line ceiling.
  [x] All 49 verify.mjs gates green (1 skipped as non-local, 10 delegated to app repos).
  [x] CHANGELOG.md updated with one line.
  [x] check-plan-integrity.mjs passed.

FOUND   
  - 26 potential duplicate entity names across schema files (warnings only, not errors). 
    These are pre-existing and not blocking. Filed for awareness — see check-schema-size.mjs output.
  - D013 was live in reusable-ci.yml (if: hashFiles on integration tests step). Fixed in this commit.
  - breaking-changes-registry.json lacked required schema metadata. Fixed in this commit.
  - check-versioning-policy.mjs had ReferenceError in CLI block. Fixed in this commit.
